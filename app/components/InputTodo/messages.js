/*
 * InputTodo Messages
 *
 * This contains all the text for the InputTodo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.InputTodo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Input Your Todo',
  },
});
