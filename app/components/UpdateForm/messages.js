/*
 * UpdateForm Messages
 *
 * This contains all the text for the UpdateForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UpdateForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UpdateForm component!',
  },
});
